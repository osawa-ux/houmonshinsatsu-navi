# AGENTS.md — Codex 用プロジェクト指示（薄い入口）

このリポジトリの指示の**正本は同階層の `CLAUDE.md`**。
Codex はセッション開始時に必ず `CLAUDE.md` を読み、その内容に**従って**作業すること。
ルール本体は CLAUDE.md に一元化しており、本ファイルはそこへの入口にすぎない。

## このファイルの扱い
- **AGENTS.md 自体は編集しない**。ルールの追加・変更は必ず `CLAUDE.md` 側で行う。
- CLAUDE.md と矛盾する記述があれば **CLAUDE.md を優先**する。

## 普遍の安全核（CLAUDE.md を読めていない場合でも必ず守る）
- git の commit / push / `reset --hard` / force は**ユーザーが明示したときのみ**。自律実行しない。
- secrets / API キー / PII（患者情報等）の**実値を出力・log・commit しない**。
- CLAUDE.md の「触らない／下書きに留める」対象（規約・決済・auth・会員管理・中立性・canonical 等）は独断で変更しない。
- 不確実な医療・算定・法務ロジックを確定情報として断定しない。
