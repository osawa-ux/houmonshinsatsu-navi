# houmonshinsatsu-navi (gh-pages deploy repo)
# 公開対象は HTML / JSON / 画像 等。基本は build_site.py の出力をすべて track する。
# このファイルは「絶対に track してはいけないもの」だけを記載する。

# === auto-commit 第二防御（2026-05-08 追加）===
# push-pc skill から auto-commit を撤去（2026-05-08）。万が一の保険として
# 中間生成物・診断結果・バックアップを除外する。
tmp/
tmp_*.txt
tmp_*.json
audit_*.json
diagnose_*.json
*_premail_*.json
*.bak
*.before-*
push.ps1.before-*

# secrets / env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
